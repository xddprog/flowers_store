import { HeroBlock } from "@/widgets/heroBlock";

const DashboardPage = () => {
  return (
    <div className="w-full">
      <HeroBlock />
    </div>
  );
};

export default DashboardPage;
